# Checks reference card

The check contract is single-sourced in the Corpus repo — see
[`docs/reference/checks.md`](https://github.com/jcosta33/corpus/blob/main/docs/reference/checks.md)
(the data lives in `checks/checks.yaml`). This kit does not duplicate the catalogue, so the card
cannot drift out of sync with the contract.
